import { Styles } from "Styles/Styles";
import { CSSObject } from "@emotion/react";

class AtomQR_PublicStyles extends Styles {
	public content: CSSObject = {
		position: "relative",
	};
}

export default new AtomQR_PublicStyles();
